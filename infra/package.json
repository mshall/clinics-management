{
  "name": "kiorly-clinics-management-infra",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "check:ascii-descriptions": "node scripts/check-ascii-descriptions.mjs",
    "check:ascii-cfn": "node scripts/check-ascii-cfn-template.mjs",
    "synth": "npm run check:ascii-descriptions && npm run build && cdk synth && npm run check:ascii-cfn",
    "deploy": "npm run check:ascii-descriptions && npm run build && cdk synth && npm run check:ascii-cfn && cdk deploy --app cdk.out --require-approval never"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "aws-cdk": "^2.170.0",
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.4.2",
    "typescript": "~5.7.2"
  }
}
