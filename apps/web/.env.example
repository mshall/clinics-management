# Optional: absolute API base (no trailing slash). If unset, the app uses same-origin
# `/api/...` which Vite proxies to http://localhost:3000 in dev (see vite.config.ts).
# VITE_API_URL=http://localhost:3000
